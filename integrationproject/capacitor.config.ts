import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.integrationproject',
  appName: 'integrationproject',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
